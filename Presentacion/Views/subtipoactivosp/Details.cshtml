@model Presentacion.Models.subtipoactivo

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>subtipoactivo</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.descripcion)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.descripcion)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.tipoactivo1.descripcion)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.tipoactivo1.descripcion)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
