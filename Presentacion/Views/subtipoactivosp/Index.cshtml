@model IEnumerable<Presentacion.Models.subtipoactivo>

@{
    ViewBag.Title = "Sub Tipo Activo";
    ViewBag.Usuario = Presentacion.Globals.Globals.Usuario;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Ingresar Nuevo Sub Tipo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipoactivo1.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipoactivo1.descripcion)
        </td>
        <td>
            @Html.ActionLink("Modificar", "Edit", new { id=item.id }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.id }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
